// components/NotesSection.js
const NotesSection = () => {
  return (
    <div>
      <textarea
        className="w-full h-40 p-2 border rounded"
        placeholder="Employee can add detailed notes about client tasks here."
      />
    </div>
  );
};

export default NotesSection;
